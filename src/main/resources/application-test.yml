spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:ticketing_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

app:
  jwt:
    secret: test-secret-must-be-at-least-256-bits-long-for-hs256-algorithm-padding-1234567890
    access-ttl: PT15M
    refresh-ttl: P7D

